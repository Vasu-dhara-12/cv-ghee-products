apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: our-cluster
  region: ap-south-1

nodeGroups:
  - name: workers
    instanceType: m7i-flex.large
    desiredCapacity: 2
    minSize: 1
    maxSize: 3

